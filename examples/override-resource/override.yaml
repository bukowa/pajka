templates:
  service.yaml:
    metadata:
      name: "examples-override-resource-override"
    spec:
      selector:
        override: examples.override-resource
      ports:
        - port: 8080
          targetPort: 8080
